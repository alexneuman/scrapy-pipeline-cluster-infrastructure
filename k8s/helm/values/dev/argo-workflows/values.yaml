server:
  auth: false
  insecure: true
  extraArgs: ["--auth-mode=server"]
  ingress:
      enabled: true
      ingressClassName: "nginx"
      hosts: [
        "workflows.scrapeops.dev",
        "workflows.localhost"
      ]
      paths:
        - /
      pathType: Prefix
      extraPaths: []