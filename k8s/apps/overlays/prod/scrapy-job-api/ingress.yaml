apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: jobs
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - jobs.scrapeops.test
    secretName: jobs-tls