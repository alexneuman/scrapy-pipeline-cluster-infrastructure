apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: scrapy-cache-pvc
  namespace: scrapy-job
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard